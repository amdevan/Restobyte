[phases.setup]
nixPkgs = ["nodejs_22", "openssl"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

[start]
cmd = "sh -c 'until npm run migrate:deploy; do echo \"[startup] waiting for database...\"; sleep 3; done; npm start'"
